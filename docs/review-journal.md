# Review Journal

The cases below are the review handles I would use before changing the implementation.

The local checks classify each case as `ship`, `watch`, or `hold`. That gives the project a small review vocabulary that matches its algorithms focus without claiming live deployment or external usage.

## Cases

- `baseline`: `input width`, score 209, lane `ship`
- `stress`: `search depth`, score 161, lane `ship`
- `edge`: `boundary pressure`, score 219, lane `ship`
- `recovery`: `complexity`, score 277, lane `ship`
- `stale`: `input width`, score 199, lane `ship`

## Note

The useful failure mode here is a wrong decision on a named case, not a vague style disagreement.
